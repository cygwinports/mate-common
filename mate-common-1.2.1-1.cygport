inherit mate


PATCH_URI="1.1.0-cygwin.patch"

DIFF_EXCLUDES="Makefile.in aclocal.m4 configure install-sh missing"

src_compile() {
	cd ${S}
	NOCONFIGURE=1 \
	./autogen.sh
	cd ${B}
	cygconf
	cygmake
}
